plugins {
	id "java"
	id "com.moowork.node" version "1.3.1"
}

node {
	version = '8.9.4'
	npmVersion = '5.6.0'
	download = true
}

jar.dependsOn 'npm_run_build', 'npm_run_test-build'

jar {
	from 'dist/angular-ui' into 'static'
}
